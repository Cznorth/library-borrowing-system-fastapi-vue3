@startuml
!include _skin.iuml
start
:输入用户名/密码;
:提交登录;
if (表单校验通过?) then (yes)
  :后端验证凭据;
  if (验证成功?) then (yes)
    :生成JWT令牌;
    :保存令牌并跳转首页;
    stop
  else (no)
    :提示“用户名或密码错误”;
    stop
  endif
else (no)
  :提示表单校验错误;
  stop
endif
@enduml

